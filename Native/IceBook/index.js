/**
 * @format
 */

import {AppRegistry, Text} from 'react-native';
import {name as appName} from './app.json';
import TextSpok from './src/TextSpok';
AppRegistry.registerComponent(appName, () => TextSpok);
